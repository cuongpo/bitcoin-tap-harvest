# Server configuration
PORT=3001

# Blockchain configuration
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# This should be the private key of the wallet that holds the BGA tokens
# This wallet will be used to transfer tokens to players when they claim rewards
# IMPORTANT: Never commit the actual .env file with your private key to version control
